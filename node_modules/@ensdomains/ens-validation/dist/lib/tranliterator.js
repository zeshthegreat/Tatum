"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Transliterator = void 0;
var TransliterationDirection;
(function (TransliterationDirection) {
    /**
     * FORWARD means from &lt;source&gt; to &lt;target&gt; for a
     * transliterator with ID &lt;source&gt;-&lt;target&gt;.  For a transliterator
     * opened using a rule, it means forward direction rules, e.g.,
     * "A > B".
     */
    TransliterationDirection[TransliterationDirection["FORWARD"] = 0] = "FORWARD";
    /**
     * REVERSE means from &lt;target&gt; to &lt;source&gt; for a
     * transliterator with ID &lt;source&gt;-&lt;target&gt;.  For a transliterator
     * opened using a rule, it means reverse direction rules, e.g.,
     * "A < B".
     */
    TransliterationDirection[TransliterationDirection["REVERSE"] = 1] = "REVERSE";
})(TransliterationDirection || (TransliterationDirection = {}));
class Transliterator {
    constructor(map, direction = TransliterationDirection.FORWARD) {
        this.direction = direction;
        this.map =
            this.direction === TransliterationDirection.FORWARD
                ? map
                : Object.create(Object.entries(map).reverse());
    }
    transliterate(string) {
        return string
            .split('')
            .map(character => {
            return this.map[character] || character;
        })
            .join('');
    }
}
exports.Transliterator = Transliterator;
//# sourceMappingURL=tranliterator.js.map
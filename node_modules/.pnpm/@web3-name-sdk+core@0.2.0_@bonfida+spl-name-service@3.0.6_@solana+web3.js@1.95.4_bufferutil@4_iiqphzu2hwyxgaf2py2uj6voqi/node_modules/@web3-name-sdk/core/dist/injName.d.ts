declare class InjName {
    getDomainName({ address }: {
        address: string;
    }): Promise<any>;
    getAddress({ name }: {
        name: string;
    }): Promise<any>;
}

/**
 * Creates a new instance of non-EVM based chains
 */
declare function createInjName(): InjName;

export { createInjName };
